<UserControl x:Class="WPF_LCD_Test.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_LCD_Test.Views"
             xmlns:vm="clr-namespace:WPF_LCD_Test.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}" Background="#FF424242" Foreground="White">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="{DynamicResource SettingsTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Label Content="{DynamicResource DevicePortLabel}" Width="120" Foreground="White"/>
                <TextBox MinWidth="200" Text="{Binding DevicePort}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Label Content="{DynamicResource AutoConnectLabel}" Width="120" Foreground="White"/>
                <CheckBox IsChecked="{Binding AutoConnectEnabled}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Label Content="{DynamicResource LanguageMenu}" Width="120" Foreground="White"/>
                <ComboBox Width="200"
                          ItemsSource="{Binding AvailableLanguages}"
                          SelectedItem="{Binding SelectedLanguage}"
                          DisplayMemberPath="DisplayName"
                          SelectedValuePath="CultureCode"
                          SelectedValue="{Binding LanguageCultureCode, Mode=TwoWay}"/>
            </StackPanel>


            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="{DynamicResource SaveButton}" Command="{Binding SaveSettingsCommand}" Margin="5,0"/>
                <Button Content="{DynamicResource CancelButton}" Command="{Binding CancelSettingsCommand}" Margin="5,0"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>